|symbol               | Object  | Style configuration; see "Symbol Description" below for the symbol fields | null |

> Note (verified against source code): the legacy `fitSize` option has been removed in the source code; the model size is now controlled by `symbol.modelHeight` / `symbol.markerPixelHeight`.
